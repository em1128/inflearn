함수에 선언된 적 없는 변수가 사용되면,
바깥쪽 함수에서 선언되었는지 확인해서 사용함
=> 내부함수가 외부함수에 접근 가능함

파라미터를 private한 변수로 만들기 위해 함수를 바로 return을 시키고,
return에서 getter,setter를 넣어 놓을 수 있다.
(함수를 바로 return시키면 파라미터의 변수가 바로 죽기 때문에)
=> 클로저를 사용하는 방법


